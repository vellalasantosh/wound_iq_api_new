# PostgreSQL DSN (use pgx driver format)
DB_DSN=postgres://username:password@localhost:5432/wound_iq?sslmode=disable

# HTTP port
PORT=8080

# App environment (dev/prod)
APP_ENV=development

# optional: log level (debug, info, warn, error)
LOG_LEVEL=info
